- 镜像构建与推送：使用 `scripts/build-images.sh` 和 `scripts/push-images.sh` 处理多组件镜像的生命周期。
- 配置渲染：通过 `scripts/render-deploy-images.sh` 生成包含特定 Registry、StorageClass 和数据库连接的 `generated/` 部署补丁。
- 来源环境初始化：执行 `scripts/install-source-environments.sh` 自动创建并标记业务所需的分层命名空间。