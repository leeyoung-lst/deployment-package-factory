- 所有生产容器均配置健康检查（Readiness/Liveness）或启动依赖约束，确保服务有序启动。
- 敏感配置（如数据库 URL、API Token）统一通过 Secret 或环境变量注入，且 Secret 模板文件被纳入版本控制而实际文件被忽略。
- Kubernetes 资源统一使用 `app.kubernetes.io/name` 和 `app.kubernetes.io/component` 标签进行身份标识与选择器匹配。