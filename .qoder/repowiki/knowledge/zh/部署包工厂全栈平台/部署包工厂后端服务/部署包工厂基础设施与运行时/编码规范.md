- 所有配置项均通过 `os.getenv` 从环境变量读取，并提供合理的默认值或校验逻辑（如 `_positive_int`）。
- 健康检查和指标渲染通过依赖注入方式获取 Repository 实例，避免在基础设施层硬编码数据访问逻辑。
- 鉴权逻辑支持多种 Token 传递方式（Authorization Header, X-Header, Query Param），并根据请求路径动态限制查询参数的使用。