- 所有 Shell 脚本均启用 `set -euo pipefail` 以确保错误即时中断与严格模式执行。
- 脚本参数解析统一采用长选项格式（如 `--registry`, `--output-dir`），并内置 Usage 提示与必填项校验。
- 路径处理统一基于 `SCRIPT_DIR` 或 `Path(__file__).resolve().parents[1]` 动态计算仓库根目录，确保跨环境可移植性。